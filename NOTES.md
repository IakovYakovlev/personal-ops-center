## Приоритет

- [ ] Реализовать возможность создания нового чата.

## 2026-05-19

- На сегодня базовая логика чата и фиксации документа завершена.
- Фокус на завтра: Реализация RAG (поиск контекста в обработанных документах) в `services/neural-assistant`.

### Цель на завтра

- Сделать так, чтобы `POST /chat` не только сохранял вопрос в БД, но и возвращал осмысленный ответ ИИ на основе контекста документа.

### Что именно нужно реализовать

- [ ] Доработать `createForChatList` в `chat.service.ts`:
- [ ] Перед созданием ответа получать контекст из doc-intelligence (или выделенного retrieval-слоя).
- [ ] Передавать в LLM: пользовательский вопрос + релевантный контекст + системный промпт.
- [ ] Сохранять в БД не только вопрос, но и ответ ассистента (две записи в чате или согласованная модель сообщений).
- [ ] Обновлять `lastMessageAt` по факту полного цикла (вопрос + ответ).

### Архитектурный шаг (обязательно)

- [ ] Вынести интеграцию с ИИ в отдельный сервис (например, `AiResponderService` / `RagService`):
- [ ] Метод `generateAnswer({ userId, chatListId, documentId, question })`.
- [ ] Внутри: retrieval -> prompt composition -> LLM call -> post-processing.
- [ ] `ChatService` оставить как orchestration layer (валидация, ownership, транзакции, сохранение сообщений).

### Минимальный scope (если времени мало)

- [ ] MVP: retrieval по одному документу, topK=3..5, без стриминга.
- [ ] Ответ ассистента сохраняется в БД и отдается сразу в API response.
- [ ] Нормальные ошибки: нет контекста, недоступен LLM, таймаут внешнего сервиса.

### Проверки (acceptance)

- [ ] Сценарий 1: пользователь отправляет вопрос -> получает ответ ассистента (не заглушку).
- [ ] Сценарий 2: вопрос по содержимому документа -> ответ опирается на найденный контекст.
- [ ] Сценарий 3: при падении LLM/retrieval API возвращает контролируемую ошибку, чат не ломается.

### Что можно перенести на послезавтра

- [ ] Стриминг токенов ответа.
- [ ] Реранжирование контекста и гибридный поиск.
- [ ] Цитаты/ссылки на фрагменты документа в UI.
- [ ] Оптимизация latency и кеш retrieval результатов.

password: uz2xcyhiA1b
